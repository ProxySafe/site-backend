version: "3"
services:
  mysql:
    image: mysql:8.0.23
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: weboperator
    ports:
      - "127.0.0.1:3306:3306/tcp"
    volumes:
      - ./dev/mysql/:/var/lib/mysql